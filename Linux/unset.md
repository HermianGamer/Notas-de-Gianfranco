- You can use the `unset` command to unset an [[Enviroment Variable]]:

```bash
unset ENV_VAR_NAME
```

Alternatively, you can set the environment variable to an empty string:

```bash
export ENV_VAR_NAME=""
```