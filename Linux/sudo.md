  
The `sudo` keyword lets you run a command on the [[CLI]] as a "superuser". It's short for ["superuser do"](https://www.linux.com/training-tutorials/linux-101-introduction-sudo/). To use it, you'll need a password with superuser privileges and [[permissions]], which you should already have if you're the only user of your machine.

