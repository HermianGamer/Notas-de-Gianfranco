You can use the [.read()](https://python-reference.readthedocs.io/en/latest/docs/file/read.html) [[Método]] to read the contents of a file into a string:

```py
# f is a file object
file_contents = f.read()
```